---
title: "Project intro"
author: "Yoana Onishtenko"
date: '7 05 2022 г '
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r}
memory.limit(size=56000)
tcga_exp = readRDS("C:/Users/joana/Documents/tcga_tumor_log2TPM.RDS")
tcga_annot = readRDS("C:/Users/joana/Documents/tcga_tumor_annotation.RDS")
tcga_tumor_norm = readRDS("C:/Users/joana/Documents/tcga_tumor_normal_datascience_proj_2022.RDS")
genesets = readRDS("C:/Users/joana/Documents/hallmarks_genesets.rds")
```

```{r}
dim(tcga_exp)
dim(tcga_annot)
```
```{r}
brca = tcga_tumor_norm[["BRCA"]]
brca.tumor = brca[["tumor"]]
brca.norm = brca[["normal"]]
brca.annot = brca[["clinical"]]
dim(brca.tumor)
dim(brca.norm)
dim(brca.annot)
```


```{r}
class(genesets)
names(genesets)
names(genesets$genesets)
## these are the first genes of the first geneset
head(genesets$genesets[[1]])
## the element description contains a description of the genesets
head(genesets$description)
```


```{r}
library(msigdbr)
all_gene_sets = msigdbr(species = "Homo sapiens")
head(all_gene_sets)
```

```{r}
h_gene_sets = msigdbr(species = "mouse", category = "H")
head(h_gene_sets)
```



